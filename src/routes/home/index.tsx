import React from 'react';
import Splitter from '../../../src/components/splitter';
import './style.css';

const Home = () => {
	return (
		<div className="home">
			<Splitter />
		</div>
	);
};

export default Home;
